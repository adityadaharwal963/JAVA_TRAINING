import java.util.List;
import java.util.PriorityQueue;

public class MergeKSortedList {
    static void mergeList(List<Integer>[] lists){
        PriorityQueue<Integer> pq = new PriorityQueue<>();
        for(List<Integer> list: lists){
            for(Integer i : list){
                pq.add()
            }
        }
    }
}
